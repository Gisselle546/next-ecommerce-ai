# PLACEHOLDER - Replace with ExternalSecret, SealedSecret, or SOPS-encrypted secret
# Do NOT commit plaintext secrets to this repo
#
# Option A: ExternalSecrets (recommended)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: backend-secrets
#   namespace: backend
# spec:
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: SecretStore
#   target:
#     name: backend-secrets
#   data:
#     - secretKey: database-url
#       remoteRef:
#         key: /prod/ecomrest/backend/database-url
#     - secretKey: redis-url
#       remoteRef:
#         key: /prod/ecomrest/backend/redis-url
#
# Option B: SealedSecret (encrypt with kubeseal)
# kubectl create secret generic backend-secrets \
#   --from-literal=database-url="postgresql://..." \
#   --from-literal=redis-url="redis://..." \
#   --dry-run=client -o yaml | \
#   kubeseal -o yaml > backend-sealedsecret.yaml
#
# Option C: Manually create secret (for testing only)
# kubectl create secret generic backend-secrets \
#   -n backend \
#   --from-literal=database-url="postgresql://user:pass@host:5432/db" \
#   --from-literal=redis-url="redis://host:6379"
